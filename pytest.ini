[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --strict-markers
    --tb=short
    --disable-warnings
markers =
    regression: regression tests against reference outputs
    performance: performance and benchmark tests
    slow: slow-running tests
    integration: integration tests
